---
slug: /reference/global-configuration
---

# 全局配置

import React from 'react';
import ConfigDocBlock from '@site/src/components/config/ConfigDocBlock';
import GlobalSpec from '!!raw-loader!@site/config-specs/paper/paper-global.yml';
import GlobalConfigSpec from '@site/src/components/config/GlobalConfigSpec';

:::info

下面的 YAML 展示了 `paper-global.yml` 的结构和默认值。

点击叶节点可以查看该设置的说明。

:::

<ConfigDocBlock data={GlobalConfigSpec}/>

<GlobalConfigSpec spec={[
  {
    key: "settings.log-player-ip",
    description: "是否在日志中记录玩家的 IP 地址",
    default: true,
    type: "boolean"
  },
  // ... 其他配置项
]} />
