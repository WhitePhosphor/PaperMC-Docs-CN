---
title: spigot.yml
description: 对 Spigot 配置文件的说明
slug: paper/reference/spigot-configuration
tableOfContents: false
---

import ConfigNode from "/src/components/ConfigNode.astro";
import SpigotYML from "/src/config/paper/spigot.yml?raw";
import { parse } from "yaml";

:::note

以下 YAML 内容展示了 `spigot.yml` 的结构和默认值。

点击一个叶节点以查看该设置的描述。

:::

:::note

要覆盖某个特定世界的选项，请在 `world-settings` 下创建一个新键，其名称为该世界文件夹的名称。

:::

<ConfigNode data={parse(SpigotYML)} />
